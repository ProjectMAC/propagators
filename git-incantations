To get a checkout

cd to a random directory

git clone git@github.com:ProjectMAC/propagators.git

this will create a directory called propagators

to get testing stuff

cd propagators
git submodule init
git submodule update

to update from repository

git pull

to modify a file and put in repository

git add <file>
git commit -m <message>
git push

