#!/bin/sh

exec mit-scheme --compiler -heap 6000 --batch-mode --no-init-file --eval '(set! load/suppress-loading-message? #t)' --eval '(begin (load "load") (load "../core/test/test-support.scm") (flush-output) (show-time run-registered-tests) (newline) (flush-output) (%exit 0))'
